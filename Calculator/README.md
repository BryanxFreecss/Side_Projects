# Calculator

A simple command-line calculator in C++. Supports +, -, *, / on two numbers.

## Build

```
g++ -o calculator calculator.cpp
```

## Run

```
./calculator
```

Then enter an expression like `3 + 4`.
